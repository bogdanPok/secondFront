env:
  browser: true
  es2021: true
extends:
  - airbnb-base
parserOptions:
  ecmaVersion: 12
  sourceType: module
rules: {
  "no-console": "off",
  import/extensions: 0 # FIXME: remove when rule will be adjusted for new nodejs version,
}
